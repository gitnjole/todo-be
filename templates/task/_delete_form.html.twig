<form method="post" action="{{ path('app_task_delete', {'id': task.id}) }}" class="delete-form" onsubmit="return confirm('Are you sure you want to delete this task?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
    <button class="btn-danger">Delete</button>
</form>